<project name="jerklib" default="dist" basedir=".">
    <description>
        Jerklib is event-driven IRC Library that can be used to write IRC Bots/clients.
    </description>

    <property name="src" location="src"/>
    <property name="build" location="build"/>
    <property name="dist" location="dist"/>
    <property file="Jerklib.properties"/>

    <target name="init">
        <tstamp/>
        <mkdir dir="${build}"/>
        <echo file="Jerklib.properties" message="version=${DSTAMP}"/>
    </target>

    <target name="compile" depends="init"
            description="compile">
        <javac srcdir="${src}" destdir="${build}"/>
    </target>

    <target name="dist" depends="compile"
            description="Create the jar">
        <mkdir dir="${dist}"/>
        <jar jarfile="${dist}/jerklib-${DSTAMP}.jar" basedir="${build}"/>
    </target>

    <target name="run" depends="dist">
        <java classpath="${dist}/jerklib-${DSTAMP}.jar" classname="jerklib.examples.jerkbot2.Jerkbot"/>
    </target>

    <target name="clean"
            description="clean up">
        <delete dir="${build}"/>
        <delete dir="${dist}"/>
    </target>
</project>


